# --- LangGraph Core ---------------------------------------------------------
langgraph>=0.2.0
langchain>=0.2.0
langchain-core>=0.2.0
langchain-mistralai>=0.1.0
langchain-community>=0.2.0

# --- FastAPI Backend ---------------------------------------------------------
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# --- Vector DB (ChromaDB) ----------------------------------------------------
chromadb>=0.5.0
sentence-transformers>=2.7.0

# --- Caching -----------------------------------------------------------------
redis>=5.0.0

# --- LLM ---------------------------------------------------------------------
mistralai>=1.0.0

# --- AI Observability --------------------------------------------------------
langsmith>=0.1.0

# --- Data --------------------------------------------------------------------
pandas>=2.0.0
numpy>=1.24.0

# --- NLP ---------------------------------------------------------------------
tiktoken>=0.7.0
scikit-learn>=1.3.0

# --- Utilities ---------------------------------------------------------------
python-dotenv>=1.0.0
pydantic>=2.0.0
pyyaml>=6.0
httpx>=0.27.0
Pillow

# --- LiveKit & GenAI ---------------------------------------------------------
livekit-agents
livekit-api
google-generativeai

# --- Admin Auth --------------------------------------------------------------
python-jose[cryptography]>=3.3.0   # JWT tokens for admin session

# --- Legacy (keep for backward compat) ---------------------------------------
# crewai[tools]>=0.134.0,<1.0.0   # <- commented out, replaced by langgraph